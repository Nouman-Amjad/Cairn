[project]
name = "cairn-mcp-runbooks"
version = "0.1.0"
description = "MCP server for runbook search and past-incident recall"
requires-python = ">=3.12"
license = "Apache-2.0"
readme = "README.md"
authors = [{ name = "Cairn contributors" }]
keywords = ["mcp", "observability", "incident-response", "llm", "agents"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.12",
    "Topic :: System :: Monitoring",
]


dependencies = [
    "cairn-core",
    "cairn-mcp-kit",
    "httpx>=0.28",
]

[project.scripts]
cairn-mcp-runbooks = "cairn_mcp_runbooks.server:main"
cairn-ingest = "cairn_mcp_runbooks.ingest:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cairn_mcp_runbooks"]

[tool.uv.sources]
cairn-core = { workspace = true }
cairn-mcp-kit = { workspace = true }

[project.urls]
Homepage = "https://github.com/Nouman-Amjad/cairn"
Source = "https://github.com/Nouman-Amjad/cairn"

global:
  env: staging
  uiBaseUrl: https://cairn-staging.example.com

auth:
  devMode: false

vllm:
  scaling:
    minReplicas: 0
    maxReplicas: 2

ingress:
  host: cairn-staging.example.com

budgets:
  maxDailyCostPerUserUsd: "10.0"

rollout:
  enabled: true
